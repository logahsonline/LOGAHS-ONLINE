"use client";

export default function HomeClient() {
  // Minimal client placeholder so Next/TS can compile.
  return (
    <div className="min-h-screen flex items-center justify-center p-10 text-slate-700 dark:text-slate-200">
      Loading…
    </div>
  );
}

